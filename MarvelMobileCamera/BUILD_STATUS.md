# Build status

- NDI đã được loại bỏ hoàn toàn.
- Đầu ra: SRT Caller tới vMix Listener và RTMP/RTMPS tới server.
- GitHub Actions được cấu hình để build `app-debug.apk` trên mỗi lần push vào `main`.
- APK vẫn cần được biên dịch và kiểm thử trên điện thoại thật; khả năng truy cập từng ống kính và 4K/60p phụ thuộc firmware thiết bị.
